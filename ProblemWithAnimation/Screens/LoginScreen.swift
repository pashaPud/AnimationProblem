

import SwiftUI

struct LoginScreen: View {
    var body: some View {
        ZStack {
            Color.red.ignoresSafeArea()
            Text("LOGIN SCREEN")
                .font(.title)
        }
    }
}

#Preview {
    LoginScreen()
}
